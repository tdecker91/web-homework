query GetTransactions {
    transactions {
        total_rows
        data {
            id
            user_id
            description
            merchant_id
            debit
            credit
            amount
        }
    }
}
